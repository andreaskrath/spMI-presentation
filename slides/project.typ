#import "/globals.typ": *

= Project & Course

#slide[
  #block(height: 100%)[
    #align(top)[
      *Current Project*
      - Kind of opposites
        - We have little data, would like more
      - Explore how "dense" our data is
        - New usecase for TimeDC

      #pause
      *Previous Projects*
      - Trajectory simplification
      - Not synthesized data
      - Also focused on use case
        - Query-driven trajectory simplification
    ]
  ]
]

#slide[
  #block(height: 100%)[
    #align(top)[
      *Course*
      - Memformer
      - Three shared authors
      - Time series forecasting (long-term)
      - Patching
      - Channel independence
    ]
  ]
]
